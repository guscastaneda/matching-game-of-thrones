
// gotCharacters.map(function(character){
//     charDiv = $('<div></div>');

//     charDiv.append($('<h2>'+character.characterName+'</h2>'));
//     charDiv.append($('<p>'+character.house+'</p>'));
//     charDiv.append($('<p>'+character.status+'</p>'));

//     charDiv.appendTo($('body'));
// });

// div(ng-repeat="character in characters");
//     h2{{character.name}};
//     p{{character.house}};

// a(ng-click="setting('spoiler')");

// $scope.setting = function(setting){
//     game.settings[setting] = !game.settings[setting];
// }


// function(param){

// }

// (param)->

// ['apple', 'peach']

// transformed = [doSomething 'with me' for element of dataStructure]
